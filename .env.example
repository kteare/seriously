# Admin credentials
SP_ADMIN_USER=admin
SP_ADMIN_PASS=changeme

# PostgreSQL (optional — falls back to JSON files if not set)
# DB_HOST=localhost
# DB_NAME=your_database
# DB_USER=your_user
# DB_PASS=your_password
